# Testing dependencies for property-based tests
pytest>=7.4.0
pytest-asyncio>=0.21.0
hypothesis>=6.88.0

# Core dependencies (if not already installed)
fastapi>=0.100.0
pydantic>=2.0.0
python-multipart>=0.0.6